import Link from "next/link";
import { loginAction } from "./actions";
export default async function LoginPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const { error } = await searchParams; return <main className="grid min-h-screen place-items-center bg-slate-950 p-5"><section className="w-full max-w-md rounded-3xl border border-white/10 bg-white p-8 shadow-2xl"><p className="text-sm font-bold uppercase tracking-[0.18em] text-sky-700">DentalAI Premium</p><h1 className="mt-3 text-3xl font-bold tracking-tight text-slate-950">Sign in to your clinic</h1><p className="mt-2 text-sm text-slate-600">Use the staff email and password created by the clinic owner.</p>{error && <p className="mt-5 rounded-xl bg-red-50 p-3 text-sm text-red-700">Email or password is incorrect.</p>}<form action={loginAction} className="mt-7 space-y-4"><label className="block text-sm font-semibold text-slate-800">Email<input name="email" type="email" required autoComplete="email" className="mt-1.5 h-11 w-full rounded-xl border border-slate-300 px-3" /></label><label className="block text-sm font-semibold text-slate-800">Password<input name="password" type="password" required autoComplete="current-password" className="mt-1.5 h-11 w-full rounded-xl border border-slate-300 px-3" /></label><button className="h-11 w-full rounded-xl bg-sky-700 font-semibold text-white hover:bg-sky-800">Sign in</button></form><p className="mt-6 text-center text-sm text-slate-600">New clinic? <Link className="font-semibold text-sky-700 hover:underline" href="/setup">Set up the owner account</Link></p></section></main>; }
