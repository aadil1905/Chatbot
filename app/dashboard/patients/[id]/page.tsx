export const dynamic = "force-dynamic";

import Link from "next/link";
import { notFound } from "next/navigation";
import { prisma } from "@/lib/prisma";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import PatientActions from "@/components/patients/PatientActions";
import StatusBadge from "@/components/appointments/StatusBadge";

export default async function PatientPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const patient = await prisma.patient.findUnique({ where: { id: Number(id) }, include: { appointments: { orderBy: [{ appointmentDate: "desc" }, { appointmentTime: "desc" }] }, clinicalRecords: { orderBy: { visitDate: "desc" }, take: 8 }, treatmentPlans: { include: { service: true }, orderBy: { updatedAt: "desc" }, take: 8 }, invoices: { include: { payments: true }, orderBy: { createdAt: "desc" }, take: 8 }, prescriptions: { orderBy: { prescribedOn: "desc" }, take: 8 } } });
  if (!patient) notFound();
  return <div className="mx-auto max-w-6xl space-y-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><Link href="/dashboard/patients" className="text-sm text-primary hover:underline">← Patients</Link><h1 className="mt-2 text-3xl font-bold">{patient.fullName}</h1><p className="mt-1 text-muted-foreground">Patient profile, complete treatment history, prescriptions, and billing.</p></div><PatientActions patientId={patient.id} /></div><div className="grid gap-6 md:grid-cols-2"><Card><CardHeader><CardTitle>Contact details</CardTitle></CardHeader><CardContent className="space-y-3 text-sm"><p><span className="text-muted-foreground">Phone:</span> {patient.phone}</p><p><span className="text-muted-foreground">Email:</span> {patient.email || "Not provided"}</p><p><span className="text-muted-foreground">Date of birth:</span> {patient.dateOfBirth?.toLocaleDateString() || "Not provided"}</p><p><span className="text-muted-foreground">Gender:</span> {patient.gender || "Not specified"}</p><p><span className="text-muted-foreground">Address:</span> {patient.address || "Not provided"}</p></CardContent></Card><Card><CardHeader><CardTitle>Medical notes</CardTitle></CardHeader><CardContent className="whitespace-pre-wrap text-sm text-muted-foreground">{patient.medicalNotes || "No medical notes recorded."}</CardContent></Card></div><div className="grid gap-6 lg:grid-cols-2"><Card><CardHeader><CardTitle>Clinical records ({patient.clinicalRecords.length})</CardTitle></CardHeader><CardContent>{patient.clinicalRecords.length === 0 ? <p className="text-sm text-muted-foreground">No clinical records yet.</p> : <div className="space-y-3">{patient.clinicalRecords.map((record) => <div key={record.id} className="rounded-lg border p-3 text-sm"><p className="font-medium">{record.visitDate.toLocaleDateString()} · {record.chiefComplaint}</p><p className="mt-1 text-muted-foreground">{record.diagnosis || "No diagnosis recorded"}</p></div>)}</div>}</CardContent></Card><Card><CardHeader><CardTitle>Prescriptions ({patient.prescriptions.length})</CardTitle></CardHeader><CardContent>{patient.prescriptions.length === 0 ? <p className="text-sm text-muted-foreground">No prescriptions yet.</p> : <div className="space-y-3">{patient.prescriptions.map((prescription) => <div key={prescription.id} className="rounded-lg border p-3 text-sm"><p className="font-medium">{prescription.prescribedOn.toLocaleDateString()}{prescription.diagnosis ? ` · ${prescription.diagnosis}` : ""}</p><p className="mt-1 line-clamp-2 whitespace-pre-line text-muted-foreground">{prescription.medicines}</p></div>)}</div>}</CardContent></Card><Card><CardHeader><CardTitle>Treatment plans ({patient.treatmentPlans.length})</CardTitle></CardHeader><CardContent>{patient.treatmentPlans.length === 0 ? <p className="text-sm text-muted-foreground">No treatment plans yet.</p> : <div className="space-y-3">{patient.treatmentPlans.map((plan) => <div key={plan.id} className="rounded-lg border p-3 text-sm"><p className="font-medium">{plan.title}{plan.toothNumber ? ` · Tooth ${plan.toothNumber}` : ""}</p><p className="mt-1 text-muted-foreground">{plan.service?.name || plan.status}{plan.unitPrice !== null ? ` · ₹${plan.unitPrice.toLocaleString("en-IN")}` : ""}</p></div>)}</div>}</CardContent></Card><Card><CardHeader><CardTitle>Billing ({patient.invoices.length})</CardTitle></CardHeader><CardContent>{patient.invoices.length === 0 ? <p className="text-sm text-muted-foreground">No invoices yet.</p> : <div className="divide-y">{patient.invoices.map((invoice) => { const paid = invoice.payments.reduce((sum, payment) => sum + payment.amount, 0); return <Link key={invoice.id} href={`/dashboard/billing/${invoice.id}`} className="flex items-center justify-between py-3 text-sm hover:text-primary"><div><p className="font-medium">{invoice.invoiceNumber}</p><p className="text-muted-foreground">{invoice.status}</p></div><p>₹{paid.toLocaleString("en-IN")} / ₹{invoice.totalAmount.toLocaleString("en-IN")}</p></Link>; })}</div>}</CardContent></Card></div><Card><CardHeader><CardTitle>Appointment history ({patient.appointments.length})</CardTitle></CardHeader><CardContent>{patient.appointments.length === 0 ? <p className="py-8 text-center text-sm text-muted-foreground">No appointments yet.</p> : <div className="divide-y">{patient.appointments.map((appointment) => <Link key={appointment.id} href={`/dashboard/appointments/${appointment.id}`} className="flex items-center justify-between gap-4 py-4"><div><p className="font-medium">{appointment.treatment}</p><p className="text-sm text-muted-foreground">{appointment.appointmentDate.toLocaleDateString()} · {appointment.appointmentTime}</p></div><StatusBadge status={appointment.status as "Pending" | "Confirmed" | "Completed" | "Cancelled"} /></Link>)}</div>}</CardContent></Card></div>;
}
