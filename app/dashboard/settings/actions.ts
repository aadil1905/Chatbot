"use server";
import { revalidatePath } from "next/cache";
import { redirect } from "next/navigation";
import { prisma } from "@/lib/prisma";
import { ROLES, hashPassword, requireOwner } from "@/lib/auth";
export async function updateClinicAction(formData: FormData) { const owner = await requireOwner(); await prisma.clinic.update({ where: { id: owner.clinicId }, data: { name: String(formData.get("name") || "").trim(), phone: String(formData.get("phone") || "").trim() || null, email: String(formData.get("email") || "").trim() || null, address: String(formData.get("address") || "").trim() || null } }); revalidatePath("/dashboard/settings"); }
export async function createStaffAction(formData: FormData) { const owner = await requireOwner(); const fullName = String(formData.get("fullName") || "").trim(); const email = String(formData.get("email") || "").trim().toLowerCase(); const password = String(formData.get("password") || ""); const role = String(formData.get("role") || "RECEPTIONIST"); if (!fullName || !email || password.length < 10 || !ROLES.includes(role as (typeof ROLES)[number])) redirect("/dashboard/settings?error=staff"); try { await prisma.user.create({ data: { clinicId: owner.clinicId, fullName, email, role, passwordHash: hashPassword(password) } }); } catch { redirect("/dashboard/settings?error=staff"); } revalidatePath("/dashboard/settings"); }
export async function toggleStaffAction(formData: FormData) { const owner = await requireOwner(); const userId = Number(formData.get("userId")); const active = String(formData.get("active")) === "true"; if (!Number.isInteger(userId) || userId === owner.id) return; await prisma.user.updateMany({ where: { id: userId, clinicId: owner.clinicId, role: { not: "OWNER" } }, data: { active } }); revalidatePath("/dashboard/settings"); }
