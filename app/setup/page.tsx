import { setupAction } from "./actions";
export default async function SetupPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const { error } = await searchParams; return <main className="grid min-h-screen place-items-center bg-slate-950 p-5"><section className="w-full max-w-lg rounded-3xl border border-white/10 bg-white p-8 shadow-2xl"><p className="text-sm font-bold uppercase tracking-[0.18em] text-sky-700">DentalAI Premium</p><h1 className="mt-3 text-3xl font-bold tracking-tight text-slate-950">Create clinic owner account</h1><p className="mt-2 text-sm text-slate-600">This setup is available only once. Use a strong password with at least 10 characters.</p>{error && <p className="mt-5 rounded-xl bg-red-50 p-3 text-sm text-red-700">Please complete every field and use a password of at least 10 characters.</p>}<form action={setupAction} className="mt-7 grid gap-4"><label className="text-sm font-semibold text-slate-800">Clinic name<input name="clinicName" required className="mt-1.5 h-11 w-full rounded-xl border border-slate-300 px-3" /></label><label className="text-sm font-semibold text-slate-800">Owner full name<input name="fullName" required className="mt-1.5 h-11 w-full rounded-xl border border-slate-300 px-3" /></label><label className="text-sm font-semibold text-slate-800">Owner email<input name="email" type="email" required className="mt-1.5 h-11 w-full rounded-xl border border-slate-300 px-3" /></label><label className="text-sm font-semibold text-slate-800">Password<input name="password" type="password" minLength={10} required className="mt-1.5 h-11 w-full rounded-xl border border-slate-300 px-3" /></label><button className="h-11 rounded-xl bg-sky-700 font-semibold text-white hover:bg-sky-800">Create secure clinic workspace</button></form></section></main>; }
