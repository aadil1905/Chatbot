import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
import { clinicalRecordSchema } from "@/lib/validations";
import { ZodError } from "zod";
async function getId(params: Promise<{ id: string }>) { const { id } = await params; const value = Number(id); return Number.isInteger(value) && value > 0 ? value : null; }
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { try { const id = await getId(params); if (!id) return NextResponse.json({ error: "Invalid record id." }, { status: 400 }); const data = clinicalRecordSchema.partial().parse(await request.json()); const record = await prisma.clinicalRecord.update({ where: { id }, data: { ...data, visitDate: data.visitDate ? new Date(data.visitDate) : undefined, diagnosis: data.diagnosis === "" ? null : data.diagnosis, clinicalNotes: data.clinicalNotes === "" ? null : data.clinicalNotes } }); return NextResponse.json(record); } catch (error) { if (error instanceof ZodError) return NextResponse.json({ error: "Validation failed." }, { status: 400 }); return NextResponse.json({ error: "Record not found or could not be updated." }, { status: 404 }); } }
export async function DELETE(_request: Request, { params }: { params: Promise<{ id: string }> }) { const id = await getId(params); if (!id) return NextResponse.json({ error: "Invalid record id." }, { status: 400 }); try { await prisma.clinicalRecord.delete({ where: { id } }); return NextResponse.json({ success: true }); } catch { return NextResponse.json({ error: "Record not found." }, { status: 404 }); } }
